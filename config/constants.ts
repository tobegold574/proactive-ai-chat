import { DEFAULT_SETTINGS, FREE_CONVERSATIONS } from "@/lib/config"

export const defaultSettings = {
  ...DEFAULT_SETTINGS,
  systemPrompt: "",
}

export const FREE_CONVERSATIONS_LIMIT = FREE_CONVERSATIONS