/** Desktop app source + docs (plugins, releases). */
export const DESKTOP_GITHUB_REPO = "https://github.com/tobegold574/proactive-ai-desktop"

export const desktopDoc = (path: string) => `${DESKTOP_GITHUB_REPO}/blob/main/${path}`
